---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
  labels:
    app: products-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: products-service
    spec:
      containers:
      - name: products-service
        image: backend-ms-products:latest2  
        imagePullPolicy: Never
        ports:
        - containerPort: 8002
        env:
        - name: NODE_ENV
          value: "prod"
        - name: APP_SECRET
          valueFrom:
            configMapKeyRef:
              name: common-config
              key: APP_SECRET
        - name: MONGODB_URI
          valueFrom:
            configMapKeyRef:
              name: common-config
              key: MONGODB_URI_PRODUCTS
        - name: MSG_QUEUE_URL
          valueFrom:
            configMapKeyRef:
              name: common-config
              key: MSG_QUEUE_URL
        - name: EXCHANGE_NAME
          valueFrom:
            configMapKeyRef:
              name: common-config
              key: EXCHANGE_NAME
        - name: PORT
          value: "8002"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 60
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: products-service
  labels:
    app: products-service
spec:
  selector:
    app: products-service
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP